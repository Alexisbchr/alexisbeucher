<section class="section--first" id="works">
    <?php foreach($allWorks as $works): ?>
        <?php
            $url = $worksManager->getUrlByImageId($works['image_id']);
            $alt = $worksManager->getAltByImageId($works['image_id']);
        ?>
        
    <article class="first--article works">
        <div class="cards">
            <img src="<?= $url; ?>" alt="<?= $alt ?>" width="100%">
            <div class="footer--cards">
                <a href="/works/<?= $works['id'];?>" class="underline">Voir plus</a>
                <a href="<?= $works['website'];?>" class="underline">Visiter</a>
            </div>
        </div>
    </article>
    <?php endforeach; ?>
</section>
