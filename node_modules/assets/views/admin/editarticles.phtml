<div id="admin">
    <section class="section--first"  id="admin">
        <article class="first--article">        
        <form action="/admin/editarticles/<?= $articles[0]['id'];?>" method="post" enctype="multipart/form-data">
            <?php
                $url = $blogManager->getUrlByImageId($articles[0]['image_id']);
                $alt = $blogManager->getAltByImageId($articles[0]['image_id']);
            ?>
            <fieldset>
                    <legend>Modifier un article</legend>
                    <div class="field">
                        <label for="title">Title</label>
                        <div class="value">
                            <input type="hidden" name="id" value="<?= $articles[0]['id'];?>">
                            <input type="text" name="title" value="<?= $articles[0]['title'];?>">
                        </div>
                    </div>
                    <div class="field">
                        <label for="content">Content</label>
                        <div class="value">
                            <input type="text" name="content" value="<?= $articles[0]['content'];?>">
                        </div>
                    </div>
                    <div class="field">
                        <label for="timestamp">Date</label>
                        <div class="value">
                        <input type="date" name="timestamp" id="timestamp" value="<?= $articles[0]['timestamp'];?>">
                        </div>
                    </div>
                    <div class="field">
                        <label for="image">Choisir une photo</label>
                        <div class="value">
                            <input type="file" accept="image/png, image/jpeg, image/webp" name="image">
                        </div>
                    </div>
                    <div class="field">
                    <label for="alt">alt</label>
                        <div class="value">
                            <input type="text" name="alt" value="<?= $alt; ?>">
                        </div>
                    </div>
                </fieldset>
                <div class="toolbar">
                    <button type="submit">Creer l'article</button>
                </div>
            </form>
        </article>
    </section>
</div>